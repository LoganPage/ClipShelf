# Windows 1.3.7

- 新增 XLSX/XLSM 表格快速预览：只读解析工作簿，按工作表浏览；公式显示文件中保存的计算结果，不执行公式或宏。
- 完善真实文件样本、安全限制和缓存结果的回归检查；保留剪贴板历史、设置、普通文件操作与其他预览格式。
- 修复“预热相邻记录首屏”在延迟保存后重启失效的问题，并保留现有设置格式。
- Windows 10 2004 及更新版本，推荐 Windows 11；提供 x64 独立运行 ZIP，不需要单独安装 .NET。

安装方式：解压 `ClipShelf-Windows-v1.3.7-public-x64.zip` 后运行 `ClipShelf/ClipShelf.exe`，或运行包内 `install.ps1` 安装到当前用户目录。已有历史和设置不会随程序包覆盖。

验证：Release 自包含发布构建通过；文件预览 116/116、原生预览 96/96、Fluent 界面 572/572、更新校验 30/30 通过。

下载文件：`ClipShelf-Windows-v1.3.7-public-x64.zip`，79,926,775 字节；SHA-256：`55247B0EA1DE195D3D899507B0313D3ABCC98515BC76C9D39D967E1181F0499F`。ZIP 含程序、安装脚本和说明，不含用户历史、设置、测试报告或调试符号。

已知情况：自动布局检查仍有 16 项既存的滚动条命中区和 200% DPI 圆角像素断言未通过；本次表格预览改动不涉及这些布局代码。
