# Windows release records

These files preserve the source commit, package size, SHA-256 digest, test
summary, and GitHub Release URL for selected public Windows releases.

They are historical evidence, not packaging inputs. Current packages should be
rebuilt from source and verified again rather than copied from an old working
directory.
