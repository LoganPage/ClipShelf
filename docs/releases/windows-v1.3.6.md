# Windows 1.3.6

- 主列表多选背景改为短促、可中断的过渡；行分隔线同步淡出或恢复。关闭 Windows 动画效果时立即切换。
- 保留现有列表滚动距离、系统滚动行数、拖选及键盘导航逻辑；加入流畅度探针与回归检查，供后续实测。
- Windows 10 2004 及更新版本，推荐 Windows 11；提供 x64 独立运行 ZIP。

- Release 自包含构建完成，0 警告、0 错误；25 组现有检查全部通过。流畅度回归检查通过：整档滚动最大回调间隔 17.17 ms，细粒度滚动 26.60 ms，两者均无 33 ms 以上长停顿。此数据仅代表本次机器状态，不证明历史冷状态问题已完全消除。
- 下载文件：`ClipShelf-Windows-v1.3.6-x64.zip`，79,913,540 字节；SHA-256：`0A548E0F5A9B1DCEF6D6ED18DE9B5DFFF3F0511FA8C93C0880121572C6CAB473`。
- ZIP 包含程序、安装脚本和说明，不包含用户历史、设置、测试报告、旧版 ZIP 或调试符号。
- 本轮未覆盖安装用户的现有程序；测试使用独立的合成数据。完整本地测试报告保存在忽略的 `artifacts/release-1.3.6-final/`。
