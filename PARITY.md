# ClipShelf Windows 对照说明

此文档是从 `../mac-reference/Sources/ClipShelfLite/` 读取的移植规格，不代表下列功能已全部通过 Windows 验证。测试完成情况以 README 和测试结果为准。

**Windows 1.0.6 明确差异：** 下文描述的是 Mac 参考行为，不是当前 Windows 交互契约。Windows 已改为单击直接改选、失活保留选择、行内按钮仅作用本行、删除与清空分离；增加右键菜单、键盘焦点和本次运行内最近10批删除撤销。工具栏额外提供撤销，选中数量位于搜索栏右侧；移除设置中的非标准点击模式和拖后恢复期入口。搜索/列表左右边距18DIP，圆角统一为浮层8、常驻控件4、记录接缝0；窗口最外缘只由DWM绘制。右键菜单采用Win11风格，滚轮支持细分输入和连续可中断滚动。粘贴不再隐藏到托盘，托盘左键单击打开主界面，右键只显示紧凑的应用管理菜单；历史正文留在主界面。现行行为详见 README。

## 主界面

来源：`App/AppDelegate.swift`、`Views/MainView.swift`。

- Mac 初始内容区域 720 × 540，最小 680 × 520；窗口位置和大小保存。Windows 保留原生可调整窗口及标题栏。
- 背景上方工具栏内边距横向 18、纵向 14。首行间距 12，应用图标 36 × 36；标题 ClipShelf 为 17 半粗，状态说明位于下方、间距 2。
- 右侧图标顺序：截图文件夹、设置、复制、粘贴、置顶、删除。Mac 仅在发现更新时额外显示更新按钮。
- 搜索栏与首行间距 12，内边距横向 12、纵向 9，圆角 12、边框 1；占位文案「搜索文字、文件名、截图名」。
- 列表外边距横向 10、纵向 8；整组条目共享圆角 14 和边框 1。每行高 74，内容高 58、纵向内边距 8、横向内边距 12，组件间距 12。
- 缩略图 52 × 40、圆角 10；文本类型用浅蓝底、文件用浅绿底，图片等比缩放。标题 14 中等字重、最多两行；下方类型、HH:mm:ss 时间与「已置顶」使用小号次要文字、间距 8。
- 每行固定显示置顶、复制、粘贴、删除四按钮；各 36 × 32、圆角 9、图标 15。分隔线左缩进 76、右缩进 12；当前行或下一行为选中状态时隐藏分隔线。
- 空状态：42 号托盘图标、17 半粗「还没有记录」、说明「复制文字或文件，或者截一张图。」，垂直间距 10。

## 颜色和设置

来源：`Support/AppTheme.swift`、`Support/SelectionColorPreferences.swift`、`Support/AppearancePreferences.swift`、`Support/AppIconPreferences.swift`、`Views/SettingsView.swift`。

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 应用背景 | #F6F7F9 | #131519 |
| 记录背景 | #FFFFFF | #202329 |
| 搜索背景 | #FFFFFF | #1B1D21 |
| 设置背景 | #FFFFFF | #1E2127 |
| 边框 | #D6D9E0 | #383D47 |
| 文字缩略图背景 / 前景 | #E6F2FA / #1A526B | #1F3842 / #7AD6E6 |
| 文件缩略图背景 / 前景 | #E8F5EB / #1F6133 | #213D2E / #73DBAB |
| 图片占位背景 / 前景 | #FAF0E0 / #8F4D0A | #423324 / #F5C26B |
| 图片缩略图底色 | #EDF0F5 | #2B2E36 |
| 行操作按钮 | #E8EBF0，78% | #404552，90% |
| 选中行操作按钮 | 黑色 7% | 白色 13% |
| 设置遮罩 | 黑色 16% | 黑色 46% |

选中行只改变底色，浅色文字黑色、深色文字白色，元数据文字 72% 不透明度。自定义选中色在深色主题下使用 68% 不透明度。

| 选中色预设 | 浅色 | 深色 |
| --- | --- | --- |
| 冷灰蓝（默认） | #E8EFF7 | #283544 |
| Apple 蓝 | #E5F1FF | #173A5E |
| 中性灰 | #ECEDEF | #343538 |
| 淡紫灰 | #EEEAF7 | #373146 |
| 青灰 | #E6F1F1 | #243B3D |

- 设置为主窗口居中遮罩面板：宽 `min(max(windowWidth - 96, 560), 760)`、高 `max(windowHeight - 96, 360)`，圆角 18，阴影黑 18%、模糊 24、纵向偏移 12；点击外侧关闭。
- 设置内容内边距 22，六个区块间距 20，区块内部间距 10，标题 16 半粗。底部固定分隔线与右对齐「完成」按钮，横向内边距 22、纵向 14。设置滚动条仅滚动时暂显（Mac 0.9 秒）。
- 暗黑模式：跟随系统 / 关闭 / 开启，即时更新并保存。
- 图标：方案 1–4，选择区 112 × 112、圆角 16、内缩 12；选中描边 2。复用 `../mac-reference/Assets/AppIcon1.iconset/` 等现有 PNG。Mac 应用内图标切换，Dock 图标保持安装图标。
- 截图：文件夹路径、选择文件夹、在文件管理器显示。
- 历史：记录开关（默认开）；单选切换开关；多选点击已选 / 未选行为；拖选点击恢复期 0–1 秒、步长 0.1、默认 0.6；五组预设、自定义不透明颜色、恢复默认；打开存储位置、清空历史。
- 快捷键：全局呼出、取消选择、置顶选中记录三组可重新录入；Esc 取消录入。
- 启动：开机自启动及错误说明。

## 选择、键盘与操作

来源：`Views/MainView.swift`、`Support/SelectionClickBehaviorPreferences.swift`、`Models/HotKeyConfiguration.swift`。

- 单击未选择条目建立单选；单击唯一已选条目清空。默认单选后点击另一条也清空；启用「单选后点击其它记录改选该记录」后改为选择新条目。
- Ctrl 对应 Mac Command：Ctrl + 单击切换条目，Shift + 单击选择锚点至目标的连续区间。多选点击已选项默认只保留点击项，也可取消全部、只取消点击项；最后一种在剩余不超过一项时会清空（Mac 原始行为）。点击未选项默认只保留点击项，也可配置取消全部。
- 点击列表空白、改变搜索、应用失去激活状态均清空选择。
- 上 / 下箭头单选移动，无选择时从当前可见的第一 / 最后一条进入，自动将目标滚入视野。
- Ctrl + A 选择全部筛选结果，Ctrl + C 复制，Ctrl + V 粘贴。Enter 粘贴焦点处的单条。Esc 清空搜索并取消选择。Mac 默认全局 Option + Space、取消选择 Command + Esc、置顶 Command + Shift + P；Windows 应避开系统保留组合。
- Space 仅预览单选；多选不触发预览。预览窗口 Space 关闭，左 / 右箭头切换相邻记录并同步列表选择；文字预览初始 640 × 460、15 号字、内缩 18，图片预览 760 × 560、等比显示。Mac 其他文件依赖 Quick Look。
- Mac 没有双击粘贴处理；条目本身的点击用途是选择。
- 行复制 / 粘贴按钮：如果该行属于当前选择，操作全部选中条目；否则只操作该行。行置顶 / 删除只操作该行。工具栏置顶批量执行：只要存在未置顶条目就全部置顶，否则全部取消置顶。
- 工具栏删除 / Delete：有选择则删除选择，无选择则清空全部记录（Mac 原始行为）。删除记录不删除原文件。
- 鼠标拖动超过 6 像素开始连续选择；冻结列表快照避免新剪贴板内容打乱拖选；边缘 72 像素范围自动滚动（40–900 像素 / 秒）。拖选结束短暂阻止粘贴 0.45 秒；恢复期设置处理拖后误点击。

## 历史、搜索和后台功能

来源：`Stores/ClipStore.swift`、`Support/SearchMatcher.swift`、`Services/ScreenshotFolderWatcher.swift`、`Services/PasteController.swift`、`Services/PreviewController.swift`、`App/AppDelegate.swift`。

- 自动捕获文字、文件列表、图片；Mac 检查顺序为文件、文字、图片，轮询间隔 0.45 秒。文字去除两端空白，纯空白忽略。
- 历史保存本机 JSON，最多 100 条；置顶优先、各组按创建时间倒序，优先淘汰最旧未置顶；重复文字 / 同文件列表 / 同截图路径或图片数据去重。应用自身复制不产生循环记录。
- 搜索标题、完整文字、文件路径、截图原路径；支持大小写 / 全半角 / 音调归一化，中文拼音全文及首字母，子序列与模糊匹配。查询长 3–5 容许 1 次编辑，6 字符以上容许 2 次。
- 多条文字按列表顺序以换行连接。多文件与有原文件的截图合并文件列表；Mac 对混合类型可写多个剪贴板对象，不可写时退回文字路径和标题。Windows 单剪贴板对象需要明确且可预测的等价策略。
- 粘贴先更新剪贴板，再隐藏 ClipShelf，回到此前应用发送粘贴组合键（Mac 延迟 0.12 秒）。Windows 应保存用户唤出前窗口并可靠恢复焦点，避免粘贴回搜索框。
- 截图文件夹只导入监听启动附近的新文件，等待写入稳定；保留原文件，同时添加图片记录并复制到系统剪贴板。Mac 扩展 png / jpg / jpeg / heic / tiff，文件名前缀 截屏 / 屏幕快照 / screenshot / screen shot。Windows 加入 截图 和本机截图命名。
- 关闭窗口后保持后台；托盘可显示窗口、选择截图文件夹、访问最近 8 条的复制 / 粘贴子菜单、清空、退出。
- 开机启动、快捷键和所有设置需持久化。

## Windows 平台适配边界

- Command 操作改用 Ctrl，Finder 改为资源管理器，菜单栏改为通知区域托盘。保留功能与布局，采用 Windows 原生窗口按钮。
- Windows 三指触控板手势可能由系统保留，应用提供鼠标按住拖选；不声称能接管系统三指手势。
- 不将 macOS GitHub Release 安装包作为 Windows 更新提供。只有存在明确的 Windows 下载资产时才能提供对应更新入口。
- macOS Quick Look 没有直接对应 Windows 系统接口；文本和图片内置预览，其他文件采用明确的文件信息 / 打开原文件功能，并据实说明差异。
- Windows 快捷键可能已被系统或其他程序注册；录入和注册时应报告冲突并保持先前可用配置。避免将 Ctrl + Esc（开始菜单）、Alt + Space（窗口菜单）当作不冲突的默认值。
- Windows HEIC 等编码器随系统安装情况变化；无法解码的图片应显示可理解说明，不让后台监听异常中断。
